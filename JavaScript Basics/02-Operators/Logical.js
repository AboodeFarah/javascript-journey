

let isStudent = true;
let hasPermission = false;

// AND &&
console.log(isStudent && hasPermission);  //false

// OR ||
console.log(isStudent || hasPermission);  //true

// Inver !
console.log(!isStudent);  //false
console.log(!hasPermission);  //true