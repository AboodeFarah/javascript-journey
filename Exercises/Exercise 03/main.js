// Create two boolean variables isSunny and isWeekend. Use logical operators to determine if you can go to the beach (both need to be true) and log the results.

let isSunny = true;
let isWeekend = true;

console.log("can you go to the beach?", isSunny && isWeekend);